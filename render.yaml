services:
  # バックエンドサービス
  - type: web
    name: btpkun-backend
    env: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 4000
      - key: FRONTEND_URL
        value: https://btpkun.onrender.com

  # フロントエンドサービス
  - type: web
    name: btpkun
    env: static
    buildCommand: npm install && npm run build
    staticPublishPath: build
    envVars:
      - key: REACT_APP_API_URL
        value: https://btpkun-backend.onrender.com